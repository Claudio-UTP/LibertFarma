export interface Medico {  // Define la interfaz Medico que refleja la entidad del backend
    id: number;    // Identificador único del medico         
    nombre: string;     // Nombre único del medico
    apellido: string;  // Apellido del medico
    telefono: string;    // Telefono del medico   
    especialidad: string;  // Especialidad del medico   
    email:string;  // Correo electrónico del medico
  }